---
status: unconfigured
company_name: ""
configured_on: ""
sources_used: []
---

# Brand Profile

**This profile is not configured yet.** The `company-design` skill has never been set up.

When the skill next triggers it will detect `status: unconfigured` above and run the
one-time onboarding flow (see `onboarding.md`). Onboarding replaces everything below this
line with the company's real brand values and flips `status` to `configured`.

Do not apply any branding while the profile is in this state — there is no brand to apply
yet.

---

<!--
TEMPLATE — this is the structure onboarding fills in. When configured, the file looks like
the example below, with real values and `status: configured` in the frontmatter. Keep the
section headings stable so `applying-brand.md` can rely on them.

## Company

- **Name:** Acme Robotics
- **One-line description:** Industrial automation hardware and software.
- **Industry:** Manufacturing / robotics

## Colors

| Role        | Name          | HEX       | Use it for                                                  |
|-------------|---------------|-----------|-------------------------------------------------------------|
| Primary     | Acme Blue     | `#1B3A8F` | Headings, logo wordmark, key accents, links                 |
| Secondary   | Acme Slate    | `#5B6B7F` | Supporting fills, secondary headings                        |
| Accent      | Acme Orange   | `#F2641E` | CTAs, highlights, small accent rules — used sparingly       |
| Neutral dark| Ink           | `#1A1A1A` | Body text on light backgrounds                              |
| Neutral light| Cloud        | `#F4F5F7` | Section panels, soft backgrounds, table zebra striping      |
| Background  | White         | `#FFFFFF` | Default canvas                                              |

**Data-visualization series order** (colors for chart series 1, 2, 3, …):
`#1B3A8F` → `#F2641E` → `#5B6B7F` → `#7FA8D9` → `#F2A66B` → `#9AA7B5`

## Typography

- **Primary typeface:** Poppins — used wherever the font is available/licensed.
- **Fallback typeface:** Arial — used for PowerPoint, Word, Excel, HTML, and any output
  where the primary font may not be installed. This must be a web-safe / universally
  available font.
- **Heading weight:** Bold (700). **Body weight:** Regular (400).
- **Heading case:** Title Case (record if the brand uses ALL CAPS headings instead).

## Logo

Logo files live in `assets/logos/`. Record one variant per background type so the right
file is picked automatically.

| Variant            | File                              | Use on                          |
|--------------------|-----------------------------------|----------------------------------|
| On light background| `assets/logos/logo-on-light.png`  | White / light backgrounds        |
| On dark background | `assets/logos/logo-on-dark.png`   | Dark / brand-color backgrounds   |
| Mark only (icon)   | `assets/logos/mark.png`           | Favicons, tight spaces, watermarks|

- **Clear space:** keep a margin around the logo equal to roughly the height of the logo
  mark / first letter; no other element enters that zone.
- **Minimum size:** ~100 px wide for the full logo; below that use the mark only.
- **Never** recolor, stretch, rotate, add effects to, or redraw the logo. Embed the actual
  file.

## Layout & voice notes

- Free-text notes captured during onboarding: signature layout patterns, whether the brand
  is minimal vs. bold, light-mode vs. dark-mode default, photography style, anything from a
  brand manual worth remembering. Optional but useful.

## Provenance

- **Configured on:** 2026-05-20
- **Sources used:** company website (acme.example.com), uploaded logo PNG, brand manual PDF
- **Confidence notes:** Colors and logo verified from the brand manual. Fallback font is an
  assumption — the brand manual did not name a fallback. Confirm if it matters.
-->
