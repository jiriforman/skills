# Tasks — Weekly Planning

> Single source of truth for Jiří's weekly tasks. Edited by the `weekly-planning` skill; safe to hand-edit too — keep the inline-attribute format.
> Last updated: {{LAST_UPDATED}}

## Format

Each task is one bullet with inline attributes in backticks:

```
- [ ] **Title** — `priority:critical|high|medium|low` `due:YYYY-MM-DD` `cat:#tag` `status:planned|in-progress|done|dropped|carried-over` `who:email` `src:email|teams|meeting|manual`
  Notes: free text. Evidence: <ms-outlook://...> or chat link.
```

Checkbox `[x]` is informational; the source of truth is the `status:` attribute.

## Active

<!-- Highest priority first. Critical tasks at the very top. -->

- [ ] **{{EXAMPLE_TASK_TITLE}}** — `priority:high` `due:{{DUE_DATE}}` `cat:#transformation` `status:planned` `who:kukura.peter@cetin.cz` `src:email`
  Notes: example task — replace on first real edit.

## Carried-over

<!-- Tasks rolled forward from previous weeks. -->

## Recently Done

<!-- Tasks completed in the current ISO week. Move to archive on next recap. -->

## Dropped

<!-- Explicitly dropped tasks, with a one-line reason. -->
